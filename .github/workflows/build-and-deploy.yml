name: Build and Deploy

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: mymindstorm/setup-emsdk@v14
        with:
          version: 3.1.64

      - name: Cache compiled dependencies
        uses: actions/cache@v4
        with:
          path: build/deps
          key: deps-${{ hashFiles('build/build.sh') }}

      - name: Build WASM
        run: npm run build

      - name: Copy dist to web and stamp cache-busting hash
        run: |
          cp dist/normaliz.js dist/normaliz.wasm web/
          WASM_HASH=$(sha256sum dist/normaliz.wasm | cut -c1-8)
          sed -i "s/__WASM_HASH__/$WASM_HASH/" web/worker.js

      - name: Upload Pages artifact
        if: github.ref == 'refs/heads/master' && github.event_name == 'push'
        uses: actions/upload-pages-artifact@v3
        with:
          path: web

  deploy:
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
